# Lumo Task — Product Roadmap / 路线图

> Single source of truth for the autonomous engineering loop (`lumo-auto-engineering-loop`, every 2h) and for planning. Each run picks **one** item — top-down by phase — and finishes it through the full flow (PM → contract → TDD → QA → gates → one-round code-review → CI → merge), then updates this file.

## Vision / 愿景
A genuinely delightful, trustworthy personal-productivity app people love and tell their friends about. **Make the fundamentals beautiful and rock-solid first**, then layer on **differentiated, viral-worthy** features — never feature-bloat over polish.

精致的体验 + 稳固的后台,再叠加有创新、能传播的功能。

## Prioritization — strict order (Jalen 2026-06-24) / 优先级
1. **Phase 1 — 精致 / 体验 / 界面 (Polish & UX).** Make what already exists feel premium. **← current focus.**
2. **Phase 2 — 安全 / 稳定 (Security & Stability).** Backend hardening + reliability, production-grade at scale.
3. **Phase 3 — 创新功能 (Innovative features).** Only after 1 & 2 feel done. Features must be **differentiated + have viral / "未来能火" potential** — these are **product decisions**: the loop writes a crisp PRD here and **waits for Jalen**; it does not auto-build large features.

The loop prefers the **highest unchecked item in the lowest-numbered phase** that is low-risk and finishable in one run. Genuinely urgent security/stability fixes may jump ahead.

---

## Phase 1 — Polish & UX  ⬅ current focus
Grounded in the current app (12 pages; `ErrorBoundary` + `ToastStack` exist; pet/gamification system present).

- [x] **Route-level code splitting** — `React.lazy` + single `<Suspense>` per page + vite `manualChunks` (`react-vendor`/`html2canvas`/`msal`). Entry bundle ~800KB monolith → `index` ~168KB; `html2canvas` (201KB) & `msal` (236KB) now lazy. Extracted `PersonAvatar` into its own module so eager call sites no longer drag the Settings chunk into the entry bundle. `RouteFallback` loading state (a11y `role=status`/`aria-live`, reduced-motion aware). *(PR #71, merged 2026-06-25)*
- [x] **Loading skeletons** — content-shaped shimmer placeholders replace the bare `Loading…` text on **Today** & **Stats**. New `Skeleton`/`SkeletonScreen` primitives (token-driven, `aria-hidden` bars inside one polite `role=status`/`aria-busy` region announcing localized `app.loading`; honors OS `prefers-reduced-motion` + in-product `reducedMotion`, mirrors `RouteFallback`) + `TaskListSkeleton`/`StatsSkeleton`. 13 component tests (status semantics, decorative-hidden, reduced-motion, row counts). Pure presentational → DFX matrix unchanged (a11y covered by tests). *(PR #93, merged 2026-06-26)* — extended to **habits** (`HabitListSkeleton`, PR #285) and **projects** (`ProjectListSkeleton` grid — kills the cold-load empty-state flash by rendering while `!loaded && projects.length===0`; store `load()` flips `loaded=true` on failure so a failed load falls back to empty-state + error toast, not an infinite skeleton; PR #293, merged 2026-07-04). Remaining: `completed` page still uses plain text.
- [x] **Client request resilience** — `web-app/src/api/client.ts` already wraps every request in an `AbortController` timeout with limited retry for idempotent reads on network errors / transient (gateway/overload) statuses; writes never auto-retry; 500 deliberately non-retryable. No infinite spinners on slow networks. *(verified shipped — #54 closed 2026-06-27)*
- [x] **Empty-state audit** — reusable `EmptyState` primitive (accent icon + title + subtitle + optional CTA; `role=status`, icon `aria-hidden`, reduced-motion safe) applied to Stats (→ matrix CTA), Settings→Members (→ add-member CTA), and Countdown (migrated to shared style). i18n EN/ZH; 5 component tests. *(PR #120, merged 2026-06-27)*
- [x] **Error-state UX audit** — swept stores + page/modal/component catch sites: store mutations (tasks/habits/people/countdown) and AI actions already surface localized `toast.error`; planning modals (Morning/Evening/Weekly) + FocusPage delegate to store toasts by design. The one genuine silent gap — `ShareCard`/`WrappedCard` PNG export swallowed *all* failures — now distinguishes a dismissed share sheet (`AbortError`, stay silent) from a real export error (toast). New `utils/share.ts` helper + tests. *(PR #121)*
- [x] **i18n completeness** — sweep remaining pages for hardcoded/untranslated EN or ZH strings. *(progress: `MatrixPage` copy localized — PR #101; static key-presence guard #108 + render-layer Playwright guard #109 catch bare keys; CJK-attribute guard #134; 8 hardcoded English `aria-label`s localized — PR #141; SettingsPage literals routed through `t()` — PR #147; ShareCard/WrappedCard busy ternaries — PR #127.)* **Audited clean 2026-06-28:** `FocusPage` already fully `t()`-driven (37 calls, 0 literals, guard green) — the stale "offender" note was wrong. Remaining `locale === "zh" ? …` ternaries elsewhere are legitimate formatters / LLM prompts (both languages render), **not** bugs; a blanket CJK-literal lint would false-positive on those, so left as-is.
- [x] **Accessibility pass** — (a) `:focus-visible` ring via shared token — PR #132; (b) `aria-label` on every icon-only button — PR #129/#141; (c) focus-trap + `Esc`-to-close + restore-focus for all modals + command palette (`useModalA11y`) — PR #130; (d) WCAG-AA contrast on accent-on-surface text — PR #133. All four sub-items shipped.
- [ ] **Visual consistency** — spacing/typography/color-token audit; tighten modal/card/topbar rhythm. Inventory ad-hoc `px` values vs design tokens; converge on the spacing scale; align card padding/radius/shadow across Today/Matrix/Stats/Settings.
- [x] **Micro-interactions** — purposeful transitions, all gated on `reduced_motion`: ~~task-complete check animation~~ (PR #150), ~~quadrant drag settle~~ (PR #151 — `useDragSettleStore` + `matrixCardSettle`), ~~pet reaction~~ (PR #155 — lightweight `react()` pulse on every completion, not just Q1; race-safe, message-less), ~~toast enter/exit easing~~ (shipped — `ToastStack`), page/route cross-fade (RouteFallback). All auto-collapse under reduced motion. Item complete.
- [ ] **Mobile/responsive refinement** — verify Pixel-5-class layouts on every page; fix overflow / tap-target issues; check bottom-tab safe-area insets, modal full-height on small screens, horizontal scroll on Matrix/Stats. *(progress: safe-area insets — PR #148; tab-bar — PR #136/#137; **TaskDetailModal full-height/scroll on short screens — PR #154**. Tap-targets: base `.btn` is 36px ≥ WCAG-AA 24px, not a bug. Remaining: habit modals' short-screen scroll are deferred to the #96 lane; per-page Playwright mobile smoke.)*

## Phase 2 — Security & Stability
From the production-readiness review (#46) + ongoing. Already shipped: #34/#36/#38/#40/#42/#45/#50 (SSRF×2, JWT/crypto, brute-force/timing, indexes+health, liveness/readiness, fail-fast secrets) and pagination #48.

- [ ] **Pagination generalization** — apply `lib/cursor` + `{items,nextCursor}` (PR #48) to `completed` / `people` / `habits` / `countdowns`; update each consumer + e2e mocks. *(**#180** tracks `people` specifically: `GET /v1/people` still returns a bare unbounded array — needs `PersonListQuery/Response` schemas, limit default 50/max 200, keyset cursor + `INVALID_CURSOR`, and migrating `api.listPeople` + `ai-tools.list_people` consumers off the bare array.)*
- [x] **Structured logging + request correlation** — JSON-line logger + per-request `x-request-id` (safe-echo or fresh UUID) + error id surfaced from `app.onError` in the 500 body for tracing. PR #153.
- [ ] **Error tracking (Sentry/OTel)** — capture + alert on backend exceptions. *(request-id correlation from #153 is the foundation.)*
- [ ] ~~**Idempotency keys**~~ — **dropped.** Deliberately removed as over-design in the P0/P3 sync simplification (the `idempotency_keys` table was dropped in #107). Do not re-add without revisiting that decision with Jalen.
- [x] **N+1 / batch writes** — `ai/classify` now flushes all per-task `ai_suggest` UPDATEs as a single atomic `db.batch`. PR #152.
- [x] **Refresh tokens / shorter access-token TTL** — rotation + reuse-detection + password-change invalidation, hash-only storage (C-P1 backend #168); transparent refresh-on-401 with access TTL cut 7d→30m, single-flight concurrent refresh, logout revokes (C-P2 #175).
- [ ] **Versioned migrations** — recorded, ordered, reversible runner (replace ad-hoc `CREATE IF NOT EXISTS`).
- [x] **DNS-rebinding SSRF residual** — IP-pinning undici dispatcher re-resolves + re-validates the host at connect time, closing the verify→connect TOCTOU rebind window; real-network verified (public host OK, localhost-resolving public domain blocked). PR #163.
- [x] **DB backup / PITR** — logical INSERT-dump (`npm run backup`, read-only) + restore runbook (`docs/ops/database-backup.md`); scheduled-CI workflow documented for opt-in. PR #156. *(Turso PITR itself remains a paid-tier option.)*
- [ ] **SQLite single-writer ceiling** — ADR on Postgres / per-tenant sharding before large-scale rollout (plan-only; needs Jalen).

### Test & quality infrastructure
- [x] **Daily integration + DFX regression** — `integration-regression.yml` runs backend real-API + DFX (Design-for-X) + web real-backend suites daily against a fresh ephemeral env (never on push/PR). DFX suite (`backend/src/test/dfx.integration.test.ts`) covers security / robustness / recoverability / observability / scalability / interoperability; coverage tracked in `docs/testing/dfx-coverage-matrix.md`. *(Jalen 2026-06-25)*
  - **Convention (auto-replenish):** every feature PR extends the integration + DFX suites and the coverage matrix for any new/changed endpoint; the loop also periodically audits API-surface vs DFX coverage and opens gap-filling PRs.
  - **Outstanding DFX test-gap backlog** *(filed audit findings not yet implemented — no matching DFX case + no matrix entry as of 2026-07-05; the loop's periodic gap-audit lane picks these off one PR at a time):*
    - [x] **#234** — server-side task search `GET /v1/tasks?q=`: tenant-scope + `LIKE` wildcard/`ESCAPE` robustness. *(PR #342, merged 2026-07-06 — 3 DFX cases, mutation-tested perfect specificity; test+docs only.)*
    - [x] **#263** — `DELETE /people/:id` → `tasks.assignee_ids` cascade/orphan handling. *(PR #346, merged 2026-07-06 — 2 DFX Reliability cases over real HTTP + real file SQLite; test+docs only.)*
    - [x] **#260** — `/v1/settings` AI-provider credential confidentiality (`enc:v1` at-rest / never echoed in cleartext). *(PR #360, merged 2026-07-07 — 3 DFX Security cases over real HTTP + real file SQLite, mutation-tested perfect specificity; test+docs only.)*
    - [x] **#284** — `auth/refresh` rotation + reuse-detection now has *daily real-HTTP* DFX coverage. *(5 DFX Security cases over real HTTP + real file SQLite: rotation/single-use, theft-response family-revoke, robustness 401/400, signout-revoke, password-change session-version invalidation; mutation-tested perfect specificity — family-revoke removal reddens exactly the successor-dies case, session-version guard removal reddens exactly the password-change case; test+docs only, handler already correct.)*
    - [x] **#304** — project OKR/KPI goal field bounds (`target`/`current`/`start` finite/nonnegative, `unit` length). *(Already covered in the daily suite — "DFX · Robustness — bounded OKR/KPI goal fields on /v1/projects (#304)" block, 4 cases; checkbox was stale.)*
    - [x] **#305** — `/v1/ai/parse` NL-capture input bounds (robustness). *(PR #368, merged 2026-07-07 — 4 DFX robustness cases over real HTTP + real SQLite, mutation-tested perfect specificity; test+docs only.)*
    - [x] **#319** — task `due` + `scheduled_start` datetime-format bounds (`scheduled_start` absent from suite). *(PR #335, merged 2026-07-06 — 8 DFX robustness cases, mutation-tested perfect specificity; test+docs only.)*
    - [x] **#320** — `/v1/ai/chat` request-payload bounds. *(Already covered in the daily suite — "DFX · Robustness — /v1/ai/chat request-payload bounds (#320)" block, 3 cases incl. at-boundary teeth; checkbox was stale.)*
    - [x] **#380** — `/v1/ai/chat` `context.todayTasks` / `recentCompleted` nested-payload bounds (sibling of #320, which covered only `messages`/`content`). *(PR #381, merged 2026-07-07 — 4 DFX robustness cases over real HTTP + real file SQLite: over-cap `todayTasks` array / over-length `todayTasks.0.title` (+recoverability) / over-cap `recentCompleted` array / at-both-caps 50-20 accepted; mutation-tested perfect specificity; test+docs only, contract already correct.)*
    - [ ] **#317** — bulk-import `/migrate` arrays are unbounded; needs a `.max()` cap. **⚠ needs Jalen** — the cap value is a migration-safety product/contract decision, not a pure test-gap.

### Awaiting Jalen — do not auto-build
- **/register account enumeration (#46 #4)** — A: accept trade-off (signin timing is defense-in-depth) vs B: email-verification flow. *Product/contract call.*

## Phase 3 — Innovative features (propose-only; needs Jalen's go-ahead)
Lead with the app's **unique hooks** — the virtual-pet/gamification system and shareable stats — the strongest "未来能火" / viral levers. The loop drafts a PRD and pings Jalen; it does not build these unilaterally.

- [~] *(**#174**, v1 shipped → v2 remains)* **Pet companion depth v2** — v1 (PR #199: productivity-aware hover voice + per-species hover greetings + streak-milestone trigger) is live. **Remaining for v2:** a happiness/energy meter with idle decay + completion recovery (store is XP-only + 3 moods today); species-distinct streak *evolution* (currently a generic string); per-species personality beyond hover copy (celebrations/status/streak copy are still universal dog copy); feed/play/pet micro-interactions.
- [~] *(**#171**, v1 shipped → yearly remains)* **Shareable "productivity wrapped" / streak cards** — milestones (`StreakMilestoneCard`, once-per-milestone), monthly Wrapped, and a re-accessible Recaps gallery (PRs #193/#196/#198) all shipped. **Remaining:** *yearly* Wrapped (`computeYearStats` + yearly recap kind — RecapsModal handles only week|month today).
- [~] *(**#172**, v1 + v2 time-budget & calendar-aware shipped → v2 conversational/adaptive remains)* **NL + AI planning v2** — v1 `generate_today_plan` (top-N by quadrant→due) + PetChat conversation UI shipped. **v2 done:** time-budgeting (reads `task.duration` + `available_hours`, greedy within budget — PR #365); **calendar-awareness** — `generate_today_plan` subtracts hours already booked in the user's imported calendar today (`ToolContext.calendarBusyHours`, computed client-side from the #169 V2 ICS import via `todayBusyHours`, threaded through `/v1/ai/chat` context, bounded `nonnegative().max(24)`) plus any NL-stated `busy_hours`; fully-booked days plan nothing; pure `effectiveBudgetHours` helper (13 unit + 2 integration + 1 DFX + 6 frontend cases). **Remaining for v2:** stateful conversational re-plan (context carried across turns), adaptive target from completion history.
- [~] *(**#170**, v1 shipped → reliable delivery remains)* **Per-task reminders — delivery beyond active tab** — v1 (PR #176: `remind_at` model + contract + sync + edit UI + in-tab scheduler with dedup + catch-up) shipped. **Remaining:** delivery when the app/tab is closed — Web Push (`push` handler + PushManager/VAPID subscription; `public/sw.js` handles clicks only today) and/or Electron native reminders; optional email fallback (V3).
- [ ] *(proposal)* **Light social / accountability** — optional shared focus rooms or buddy streaks.
- [ ] *(**#169**, untouched)* **Calendar interop — Google + ICS two-way** — only the pre-existing Outlook read-only display exists. **Needs, in order:** V1 ICS *export* (per-user signed/revocable `.ics` feed + `feed_tokens` table), V2 ICS *import*, V3 Google Calendar OAuth two-way. (Also covers the "Deeper integrations: Outlook + Google/ICS" line.)
- [ ] *(standard features, lower bar — confirm with Jalen)* recurring tasks · subtasks · due-date push notifications · macOS/Linux Electron builds · real-time multi-device sync · team/shared workspace · public API.

---

## Current State / 当前状态 (shipped & in production)
Today view + recommended card + CompletedTimeline · Eisenhower Matrix (drag-drop) · Focus/Pomodoro (Web Worker) · AI classify (LLM + heuristic) · server-side task search · habit check-in · 5-step onboarding · bilingual EN/ZH · accent theming · calendar week view · Stats + shareable PNG · PWA · mobile bottom-tab layout · Lumo pet celebration moments · Hono+SQLite/Turso backend, JWT auth, REST · Electron Windows app · GitHub Actions CI/CD.

## Done log
- 2026-06-24: Security/architecture hardening — #34/#36/#38/#40/#42/#45/#50; task-list pagination #48; review report #46.
- 2026-06-25: Daily integration + DFX regression workflow & suite; fixed malformed-JSON → 500 (now 400) robustness bug found by the new DFX suite.
- 2026-06-26: P1a generic sync core + desktop provisioning (#102); i18n sweep — `MatrixPage` copy localized (#101). Both merged.
- 2026-06-27: Sync line fully landed (P1b desktop client #105, P2 per-customer deploy #106, P3 cleanup #107); i18n bare-key guards (#108 static + #109 render-layer); countdown lunar/solar P1+P2 (#113/#114); settings simplification (#115/#116) + desktop cloud-base fix & README (#117) + gated Windows release pipeline (#118); empty-state audit (#120) + error-state audit (#121); i18n CJK-attribute guard (#134) + 8 hardcoded English `aria-label`s localized (#141). Phase-1 polish nearly complete: client-resilience verified shipped (#54), empty-state + error-state audits done (#120/#121); i18n sweep ongoing (remaining: ShareCard/WrappedCard ternaries, FocusPage/SettingsPage literals).

- 2026-06-28: Accessibility pass complete (focus-visible #132, icon aria #129/#141, modal focus-trap #130, AA contrast #133); micro-interactions complete (task-complete pop #150, drag-settle #151, pet reaction #155); visual radius tokens #145; mobile safe-area #148 + TaskDetailModal short-screen scroll #154; reduce-motion setting wired #149. **Phase-1 polish effectively done** (remaining: visual spacing-scale = high-churn/low-value, deferred; per-page mobile Playwright smoke; habit-modal scroll owned by the #96 lane). **Phase-2 stability underway:** N+1 batch in ai/classify #152, structured logging + request-id #153, logical DB backup + runbook #156. i18n & FocusPage audited clean. Idempotency-keys line dropped (contradicts the #107 simplification).

- 2026-06-28 (cont.): Phase-2/3 business features — per-task reminders V1 (#170 → PR #176, merged: `remind_at` model + contract + sync manifest + edit UI + scheduler fire-on-time); refresh-token rotation complete (C-P1 #168 + C-P2 #175); **task templates V1 (#173)** — new lightweight `templates` entity (JSON-payload, four-tuple syncable), save-as-template from the task ⋯ menu, a template library to browse/instantiate/rename/delete, reusing the Duplicate field-copy + progress-reset contract.
- 2026-06-29: SSRF IP-pinning (#163) + refresh-token rotation (#168/#175) + completed full-history keyset pagination (#164) all landed earlier and are now ticked above (roadmap bookkeeping). Web build hides the desktop-only Data & Sync settings tab entirely (#181) + dead web-storage branch removed (#185). **DFX integration coverage:** added `templates` to the tenant-isolation/malformed-body matrix (4 cases) and fixed the `completed/reopen` isolation test that #164's `{items,nextCursor}` pagination had latently broken — daily integration-regression now green at 42/42 (#186). **Test-gap audit (#190 → #191):** the #165 sub-resource IDOR sweep missed `POST /v1/focus/sessions` (silent IDOR — writes a `completed_entries` row + bumps the task's `pomos_done` by caller-supplied `task_id`, returns `200 {ok:true}` even cross-tenant, never 404); added a DFX state-survival case (owner's `pomos_done` unmoved + no leaked entry) and hardened the `pomos_done` UPDATE with `AND user_id` as defense-in-depth (no happy-path change). Was a test gap + latent footgun, not an active vuln.

- 2026-06-30: **Test-gap audit — DFX Scalability for `/v1/completed` (#202 → PR #203, merged).** The Scalability dimension advertised "list responses always bounded; pagination correct under volume" system-wide but only exercised `/v1/tasks`. The full-history completed log (`GET /v1/completed`, keyset-paginated since #164) — the highest-growth list — had **no** scalability coverage, and its contract *differs* from tasks (DEFAULT 200 / MAX 500, over-max `limit` **clamped** via `Math.min` rather than 400-rejected). Added 3 cases over real HTTP + real SQLite (explicit `limit` bounds page + `nextCursor`; over-max → 200 clamped ≤500 not 400; cursor walk = every entry once). One-round adversarial review verified the composite keyset `(completed_at, id)` makes the walk tie-stable (not flaky). Test + docs only — gap in the tests, not the code.

- 2026-06-30 (cont.): **Test-gap audit — DFX Security/IDOR for `POST /v1/ai/breakdown` (#209 → PR #210, merged).** The #165/#190 caller-supplied-`id` IDOR sweeps covered CRUD + sub-resource routes but never the **`/v1/ai`** surface. `POST /v1/ai/breakdown` loads a caller-supplied `taskId` to feed its title/description into the LLM prompt — a dropped `WHERE user_id` would disclose another tenant's task content into the attacker's AI response and burn the owner's cloud-AI quota. Added 2 DFX cases over real HTTP + real SQLite (cross-tenant `taskId` → 404 before any LLM call; `error.code === "NOT_FOUND"`, never 5xx). Adversarial review (2 verifiers) confirmed the `status === 404` assertion is the load-bearing guard (breaks if `AND user_id` dropped) — not a false-green. Handler verified already correct; **gap in the tests, not the code** — test + docs only, no production change.

- 2026-06-30 (cont.): **Closed out the flagged backlog DFX PR #195 (#194 — `tasks` complete/uncomplete cross-tenant IDOR).** Authored in an earlier run and left open with a stale "CONFLICTING" note; re-checked on current main — it auto-merges clean (the conflict was against an older main), all PR CI green. Before merge: re-ran the full DFX integration suite locally (55/55) and **mutation-tested both new cases** — dropping `AND user_id = :uid` from the `complete` SELECT (`tasks.ts:272`) and the `uncomplete` SELECT (`tasks.ts:346`) turns *exactly* those 2 cases red (53/55), restored → 55/55 — confirming teeth, not a false-green. One-round high-effort code-review: clean, no findings (posted as PR comment). Both handlers already scope by `user_id` → gap in the tests, not the code; test + docs only, no production change. Merged + issue #194 closed.

- 2026-06-30 (cont.): **Test-gap audit — DFX Robustness/Scalability for `/v1/projects` nested payload bounds (#225 → PR #226, merged).** The `projects` resource (#213/#219) was reached by the daily DFX suite only via the #158 tenant-isolation + malformed-JSON cases; #220 added the task→project *reference* boundary but never projects' own **nested/bounded payload** robustness. Projects carry the app's richest, growth-prone payload (nested `goals[]` + rich-text `content`) and there is **no body-size middleware**, so the Zod caps in `routes/projects.ts` (`content` ≤ 1 MB, `goals` ≤ 50, `goals[].text` ≤ 200) are the *only* bound on row growth — a loosened cap, or a 5xx on an oversized body instead of a clean 400, would slip past every existing case (sharpened by the in-flight TipTap inline-image work #222 inflating `content`). Added 3 cases over real HTTP + real SQLite, each asserting the 400 `VALIDATION_ERROR` envelope **and** the offending dotted field path (teeth): over-cap `content` → names `content` **+ a normal create still 201 afterwards** (recoverability/no-poison); over-length `goals.0.text` → names the nested element; over-cap `goals` array → names `goals`. One-round high-effort review: clean, no findings — verified the `goals` assertion is robust because `GoalSchema.done` has `.default(false)` (51 valid elements ⇒ the only error is the array `.max(50)`), not accidentally satisfied by per-element errors (posted as PR comment). Handler/contract already correct → gap in the tests, not the code; test + docs only, no production change. dfx 61/61, integration 59/59. Merged + issue #225 closed.

- 2026-07-01: **Test-gap audit — DFX Robustness for the `countdowns.date` anchor (#240 → PR #241, merged).** Follow-up to the #176 `remind_at` format-bound work, applied to the sibling scheduler/display-driving field. A countdown's `date` is the solar ISO anchor that **both** the "days until" math and the **lunar-recurrence** engine parse; its only bound is a strict date-only regex (`^\d{4}-\d{2}-\d{2}$`, create + partial update). The daily DFX suite reached `countdowns` only via the #158 tenant-isolation + malformed-JSON cases — no robustness coverage on `date`, so a regression loosening the regex to `z.string()` would let `"someday"` / a full-datetime string into the column display + recurrence later parse (→ `NaN` days-until or a crashing lunar conversion), past every existing case. Added 4 cases over real HTTP + real SQLite (malformed create → 400 naming `date`; full-datetime plausible-but-wrong → 400; valid round-trips via owner's list — no GET /:id; malformed `PATCH` → 400 with stored date unmutated). Mutation-tested: loosening the regex turns **exactly** the 3 bound-enforcing cases red (66/69), round-trip stays green — teeth confirmed. Handler/contract already correct → **gap in the tests, not the code**; test + docs only, no production change. dfx 69/69.

- 2026-07-02: **Test-gap audit — DFX Robustness for the settings reminder-time fields (#264 → PR #265, merged).** Follow-up in the #176 `remind_at` / #240 `countdowns.date` format-bound line, now on the last unscheduled scheduler-driving field family. The **`settings`** resource had **no presence in the daily DFX suite at all** (its enum + SSRF coverage lives only in the per-PR in-process `api/settings.test.ts`, not over real HTTP + real file SQLite). `morning_reminder_time` / `evening_reminder_time` on `PATCH /v1/settings` are the `HH:MM` anchors stored verbatim and returned by `GET /v1/settings` to drive the client's reminder scheduling, gated **only** by a shape regex `^\d{2}:\d{2}$` (`routes/settings.ts:30-31`) — so a regression loosening it to `z.string()` would let `"9am"`/`"9:5"` into the column, surfacing only as a never-firing reminder in prod, past every existing daily case. Added 4 cases over real HTTP + real SQLite (malformed `morning_reminder_time` → 400 naming the field; single-digit `"9:5"` near-miss → 400; valid `HH:MM` round-trips via `PATCH` then `GET`; malformed `PATCH` leaves the stored value unmutated). Mutation-tested: loosening the regex to `z.string()` reddens **exactly** the 3 bound-enforcing cases (80/83), round-trip stays green — teeth confirmed. **Settings SSRF deliberately left off this daily layer** (`baseUrl` private-IP guard early-returns under the harness's `local` dbMode → a daily case would be a false-green; stays in the in-process api suite). Handler/contract already correct → **gap in the tests, not the code**; test + docs only, no production change. dfx 83/83. One-round high-effort review clean (independently re-verified contract claims + suite + mutation teeth locally); posted as a PR comment.
- 2026-07-02: **Test-gap audit — DFX Robustness for the habit check-in `date` format bound (#267 → PR #268, merged).** Final member of the scheduler/streak-driving format-bound field family (#176 `remind_at` → #240 `countdowns.date` → #264 settings reminder-times). The `habits` resource was in the daily DFX suite only via the #158 tenant-isolation + malformed-JSON cases and the #165 log-IDOR sweep — no format-robustness coverage on its `date`, the key of every `habit_logs` row and the sole input to the client's **streak** math. That `date` is gated **only** by `^\d{4}-\d{2}-\d{2}$` on **two** surfaces: the `POST /:id/log` JSON body **and** the `DELETE /:id/log/:date` **path param** (via `LogParam`). The DELETE is the insidious one — idempotent (`204` on no-match), so a dropped param bound would slip a bad date into an unguarded `DELETE … WHERE date = :date` with **no status-code change**. Added 4 cases over real HTTP + real SQLite (malformed `date` on POST → 400 naming `date`, no log written; plausible-but-wrong full-datetime `"2026-07-02T09:30"` → 400; valid `date` round-trips via `GET /v1/habits/logs`; malformed `date` on the DELETE path param → 400 with a pre-existing check-in left intact). **Mutation-tested with perfect specificity** (independently re-verified this run): loosening the POST-body regex reddens exactly the two POST cases, loosening the `LogParam` regex reddens exactly the DELETE case, round-trip stays green. dfx 87/87. Handler regexes verified already correct → **gap in the tests, not the code**; test + docs only, no production change. One-round high-effort review clean; posted as a PR comment.
- 2026-07-02: **DFX Security — LLM-output-driven cross-tenant IDOR on the AI write surface (#249 → PR #250, merged).** `POST /v1/ai/classify` injects attacker-controllable task titles into an LLM prompt, then wrote the model's returned per-task quadrant back keyed by an id taken **straight from model output** (`suggestStmt(q, item.task_id)`, `ai.ts:180`) with no re-validation against the caller's task set — so a prompt-injection could coax the model into naming another tenant's task id and silently mutate the victim's `ai_suggest` (endpoint still returns 200). Scoped the write with `AND user_id = :uid` (+ the two `/ai/recommend` conviction/reason writes as defense-in-depth). New DFX · Security integration case (real HTTP + real SQLite + a local mock LLM server naming the victim's id) — author verified red→green (revert ⇒ victim row becomes `Q4`). One-round high-effort review: **the recommend-path guard is intentionally left without a test** — at `ai.ts:255` its write target is `q1Tasks.find(...) ?? topTask`, structurally always a caller-owned row, so a black-box IDOR test there would be a false-green (passes with or without the guard); `/classify` is the one reachable path and it has the load-bearing case. dfx 73 ✓. Findings + resolution posted as a PR comment.

- 2026-07-04: **Phase 1 (Polish/UX) — Projects loading skeleton (#292 → PR #293, merged).** On a cold load, `ProjectsPage` flashed the "no projects yet" empty state before `list()` resolved (store starts `loaded=false` with no cache). Added `ProjectListSkeleton` (auto-fill card grid mirroring the real layout, same `role=status`/`aria-busy`/`aria-live=polite` a11y contract as the other skeletons, `aria-hidden` bars, design tokens) rendered while `!loaded && projects.length===0`; hardened `useProjectsStore.load()` to mark `loaded=true` on failure (fall back to empty-state + error toast, never an infinite skeleton). 6 new tests (skeleton a11y/bar-count ×3 + page loading/empty/grid ×3). Frontend-only, no API/contract change. One-round high-effort code-review posted as a PR comment: correctness clean; 1 confirmed minor UX bug fixed (`253ac98` — mounted the create/template modals in the loading branch so a New-project click during the load window is honored immediately); 2 altitude nits deferred to the visual-consistency pass, 1 refuted. CI fully green.

- 2026-07-04 (cont.): **Test-gap audit — DFX Security id-collision guard on the top-level bulk-import migrate endpoints (#295 → PR #296, merged).** Sibling to the #276 habits/migrate audit. `POST /v1/countdowns/migrate` and `POST /v1/projects/migrate` are the two top-level bulk imports that had **zero** presence in the daily DFX suite (only the per-PR in-process api suites, in-memory). Both `INSERT` rows with a **client-supplied `id`** while forcing `user_id` from the JWT, into tables keyed on a **global `id TEXT PRIMARY KEY`** — so the *only* barrier stopping a caller from clobbering another tenant's row by supplying its id is each statement's `INSERT OR IGNORE`. The insidious part: both handlers return `migrated: <submitted>.length` (the **submitted** count), so status + count are **blind** to an `INSERT OR IGNORE`→`INSERT OR REPLACE` regression that would let an attacker **steal** a victim's countdown/project by id (OR REPLACE rewrites the row's `user_id`); only cross-tenant state-survival catches it (no GET /:id → read back via each owner's list). Added 2 cases over real HTTP + real SQLite (attacker imports one owned row = positive control + one colliding on the victim's id with different content → attacker's list gains the owned row but **never** the foreign id; victim's row survives **unmutated**). **Mutation-tested with perfect specificity** (independently re-verified this run): `OR IGNORE`→`OR REPLACE` on countdowns reddens exactly the countdowns case, projects stays green (and vice-versa). dfx 98 → 100. Both handlers verified already correct → **gap in the tests, not the code**; test + docs only, no production change. One-round high-effort review clean; posted as a PR comment.

- 2026-07-05: **Issue backlog cleanup (Jalen).** Audited all 21 open issues against merged PRs + code + the DFX coverage matrix. **Closed 6 as completed** (evidence in each): #306 (habits-row migrate id-collision guard, PR #314), #244 (ai/classify unscoped-UPDATE — handler `user_id`-scoped + mock-LLM IDOR test landed), #231 (sync push/pull isolation — covered by #255), #182 + #183 (/v1/templates tenant-isolation + integration/DFX coverage), #162 (sub-resource action-endpoint cross-tenant IDOR — covered by #165/#194). **Remaining 15 recorded in-plan:** 9 DFX test-gaps → "Outstanding DFX test-gap backlog" above (#234/#263/#260/#284/#304/#305/#319/#320/#317); #180 → Phase-2 pagination; #170/#171/#172/#174 → Phase-3 (marked `[~]` v1-shipped/v2-remains); #169 → Phase-3 calendar interop.

- 2026-07-06: **Test-gap audit finished — DFX robustness for task `due` + `scheduled_start` datetime-format bounds (#319 → PR #335, merged).** Authored by an earlier loop run and left open with CI green + a clean one-round adversarial review already posted; this run verified all checks green + review clean (`CLEAN` merge state), merged, and closed #319. 8 DFX robustness cases over real HTTP + real SQLite: `due` (`^\d{4}-\d{2}-\d{2}$`) and `scheduled_start` (`^…T\d{2}:\d{2}(:\d{2})?$`) each get malformed-create → 400 naming the field, plausible-but-wrong-shape near-miss (the other field's valid shape → 400 on this field), valid round-trip, and malformed-PATCH → 400 with stored value unmutated. Mutation-tested with perfect specificity (loosening `due`'s regex reddens exactly its 3 bound cases; all 4 `scheduled_start` cases stay green). dfx 109 → 117. Handlers/contract already correct → **gap in the tests, not the code**; test+docs only, no production change.

- 2026-07-06 (cont.): **Test-gap audit merged — DFX coverage for server-side task search `GET /v1/tasks?q=` (#234 → PR #342).** Authored by an earlier loop run and left open with CI green + a clean one-round adversarial review already posted + independently mutation-verified; this run confirmed all checks green (`CLEAN`/`MERGEABLE`), merged, closed #234, deleted the branch. The search endpoint is a **distinct SQL builder** (`routes/tasks.ts:117-124`) — its own `LIKE :q ESCAPE '\'` over `title`/`desc` in both locales with user-wildcard escaping — that the daily DFX suite never exercised (it only touched the plain list + cursor pagination). Added 3 cases over real HTTP + real SQLite: **Security** (intruder `?q=<owner keyword>` returns none of the owner's tasks though the intruder holds a same-keyword task ⇒ emptiness is *scope*), **Robustness** (literal `?q=%` returns only rows literally containing `%`, not the full tenant list; literal `?q=c_t` matches `c_t` but not `cat`/`cot`). Mutation-tested with perfect specificity (drop wildcard-escape ⇒ reddens exactly the two robustness cases; drop `WHERE user_id` ⇒ reddens the security case). dfx 117 → 120. Handler already scoped + escaped → **gap in the tests, not the code**; test+docs only, no production change.

- 2026-07-06 (cont.): **Test-gap audit merged — DFX Reliability for the person-delete → `tasks.assignee_ids` cascade (#263 → PR #346).** Authored by an earlier loop run and left open with CI green but no review round yet; this run ran the mandated one-round high-effort adversarial review (two independent finder passes — test-correctness + contract/consistency — verified clean, posted as a PR comment), confirmed all checks green (`CLEAN`/`MERGEABLE`), merged, closed #263, deleted the branch. `DELETE /v1/people/:id` (`routes/people.ts:132-167`) is the app's **only cross-resource cascade**: after tombstoning the person it rewrites `tasks.assignee_ids` via `json_each`, scoped `WHERE user_id AND EXISTS(… value = :pid)`. The daily DFX suite pinned the person *row* (#158 isolation + #279 avatar bounds) but **nothing** pinned the cascade. Added 2 DFX Reliability cases over real HTTP + real file SQLite: **AC1** — deleting a person drops its id from a referencing task, retains a co-assignee (partial removal, not a blanket clear), leaves a non-referencing task whole; **AC2** — the referencing task's `updated_at` (HLC) advances while an unrelated task's is byte-for-byte unchanged (proves the `EXISTS(… = :pid)` predicate scopes the write — no tenant-wide amplification). Review confirmed teeth against the handler (assertions redden if the cascade is neutered or the `EXISTS` scoping dropped), no flakiness (HLC strictly monotonic + lexicographically sortable), no pagination/shared-state pollution. dfx 120 → 122. Handler already correct → **gap in the tests, not the code**; test+docs only, no production change.

- 2026-07-07: **Test-gap audit merged — DFX Security for `/v1/settings` AI-provider credential confidentiality (#260 → PR #360).** Authored by an earlier loop run and left open with CI green but **no review round yet**; this run ran the mandated one-round high-effort adversarial review (two independent finder passes — line-by-line test correctness + soundness/isolation — the one substantive candidate, an "empty-string ciphertext ⇒ vacuously-true `!includes`" false-green, was **REFUTED**: each `includes(ciphertext)` / `includes(after)` is guarded by a preceding `startsWith("enc:v1:")` precondition assert in the same test, so the empty-string path is unreachable; the shared-actor inter-test dependency matches the file's established DFX convention and fails loudly. Posted as a PR comment.), confirmed all checks green (`CLEAN`/`MERGEABLE`), merged, closed #260, deleted the branch. `PATCH /v1/settings` with `ai_configs_update.key` is the **one** endpoint where a user hands the backend a long-lived secret (their provider API key); its confidentiality contract had no real-HTTP + real-file-SQLite coverage (settings' only prior daily presence was the #264 reminder-time cases; the per-PR in-process `api/settings.test.ts` asserts `hasKey` shape but not at-rest ciphertext or full-body non-echo). Added 3 DFX Security cases over real HTTP + real file SQLite (raw `ai_configs` column read back via `queryOne` on the shared db client): **AC1** — a submitted key is stored `enc:v1:`-prefixed (AES-256-GCM via `encryptSecret`, `routes/settings.ts:150`) and the plaintext sentinel is absent from the column; **AC2** — a full-body substring scan proves neither the plaintext key nor its stored ciphertext appears in the `PATCH` **or** `GET` response, and the projected config carries no `key` field at all (only `hasKey:true`, `rowToSettings` `routes/settings.ts:62-70`); **AC3** — a PATCH touching only `model` leaves the key `enc:v1:`-encrypted at rest and un-echoed (the handler's `if (key != null && key.trim())` guard neither wipes nor round-trips it back to cleartext). Mutation-tested with perfect specificity (bypass `encryptSecret` ⇒ reddens all 3; add `key` to `rowToSettings` ⇒ reddens exactly AC2+AC3, AC1 stays green). dfx 122 → 125. Handler/crypto already correct → **gap in the tests, not the code**; test+docs only, no production change.

- 2026-07-07 (cont.): **Phase 1 (Polish/UX) — task-detail Pomodoro label i18n + Save-as-Template loading feedback (PR #362, merged).** Authored by an earlier loop run and left open with CI fully green + a completed one-round `/code-review high` (0 findings, posted as a PR comment); this run confirmed all 30 checks green (`CLEAN`/`MERGEABLE`), merged, deleted the branch. Two independent detail-surface defects from a real-defect sweep: (1) `TaskDetailModal.tsx:222` carried the file's **only** hardcoded English literal (`Pomodoro`) though it `t()`s 37× and the app already renders pomodoro → 番茄钟 everywhere else — zh users saw stray English; routed through `t("detail.pomodoro")` (en/zh key added, parity green). (2) `ProjectDetailPage` **Save-as-Template** was `disabled` while saving but showed no spinner — the exact "clicked but no feedback" class the loading-indicator standard #298/#299 fixed; swapped the leading icon for `<Spinner size={12}/>` + `aria-busy` while pending, matching the WeekFocus "add today" button. Pure frontend, no contract/endpoint/migration; new i18n key is static `t()` (no dynamic family). Gates green: changed-file tsc 0 · vite build ✓ · i18n+standards 36/36 · ProjectDetailPage 6/6.

- 2026-07-07 (cont.): **Test-gap audit — DFX Robustness for the `/v1/ai/parse` NL-capture input bounds (#305 → PR #368, merged).** The natural-language quick-capture endpoint (sibling of the IDOR-covered `/ai/breakdown` #209 / `/ai/classify` #249, and the foundation of the Phase-3 NL+AI-planning proposal) had its **input bounds** uncovered by the daily DFX suite. Its body is validated by `validate("json", ParseBody)` (`routes/ai.ts:300-303`) **before** any LLM call, gated by `text: z.string().min(1).max(500)` (the free text injected verbatim into the prompt — `max(500)` is the only bound on how much attacker-controlled text reaches the model, `min(1)` rejects empty capture) and `locale: z.enum(["en","zh"]).optional()`; a regression loosening either would slip past every existing case, surfacing only as runaway LLM cost / a malformed prompt in prod. Exploited the endpoint's graceful **no-LLM fallback** (no provider → a valid request returns `200 {title,quadrant:"unclassified",confidence:0}`) so both the 400 and 200 paths verify with no AI provider. Added 4 cases over real HTTP + real file SQLite (dedicated actor to decouple from the per-user classify rate-limit): over-cap `text` → 400 naming `text`; empty `text` → 400 naming `text`; out-of-enum `locale` → 400 naming `locale`; valid `text` → 200 graceful fallback (title echoes the trimmed input). **Mutation-tested with perfect specificity:** loosening `text` to `z.string()` reddens exactly the two `text` cases; loosening the `locale` enum reddens exactly the `locale` case; the fallback stays green. dfx 125 → 129. Handler/contract already correct → **gap in the tests, not the code**; test+docs only, no production change. One-round high-effort review clean; posted as a PR comment.

- 2026-07-07 (cont.): **Test-gap audit — DFX Security for the public calendar iCal feed capability (#384 → PR #385, merged).** The shipped-in-#169 feed `GET /v1/calendar/feed.ics?token=…` is the app's **only** public, unauthenticated, token-as-capability endpoint — the opaque token *is* the read capability for a user's open due-tasks + countdown events (the Google/Apple "secret iCal address" model), resolved through a `calendar_feed_token_hash` reverse lookup (`routes/calendar.ts:98-102`), a **distinct non-JWT auth path** that had no presence in the daily DFX suite (only the per-PR in-process `api/calendar.test.ts`). A dropped guard here silently becomes a cross-tenant leak, and PR CI never runs the DFX suite. Added 3 DFX Security cases over real HTTP + real file SQLite, each with positive controls (emptiness alone can't false-green): **AC1 capability isolation** — Alice's feed (her token) contains her task and **not** Bob's, and vice-versa (guards the feed queries' `WHERE user_id = :uid`); **AC2 anti-enumeration** — a missing token and an unknown/garbage token both → the **same** `404` (no 401-vs-404 validity oracle), a valid token → `200 text/calendar`; **AC3 rotation = revocation** — after `POST /v1/calendar/feed/rotate` the old token → `404` and the new token → `200`. dfx 129 → 132. Handler already correct (`rotateToken` overwrites the hash unconditionally; missing/unknown/revoked all funnel through the shared `!user` 404) → **gap in the tests, not the code**; test+docs only, no production change. One-round high-effort review clean (verified route semantics match all 3 ACs + no cross-test count pollution on the shared `alice`/`bob` fixtures); posted as a PR comment.

- 2026-07-07 (cont.): **Test-gap audit merged — DFX Robustness for the auth `register`/`signin` input bounds (#387 → PR #388, merged).** Authored by an earlier loop run and left open with all 30 checks green + a completed one-round high-effort adversarial review (0 real defects — assertions verified to have teeth against `validate.ts`'s dotted-path normalization, AC4 ordering guaranteed by middleware registration before the handler, no cross-test pollution; posted as a PR comment); this run confirmed `CLEAN`/`MERGEABLE`, merged, closed #387, deleted the branch. The app's two **public, unauthenticated, highest-traffic** endpoints were exercised by the daily suite only as the `registerUser` fixture — the sole input assertion was "weak password rejected at registration". `register` never had its `email` shape / `name` length bounds pinned, and `signin` had **zero** validation coverage. Both bodies are gated by a Zod schema (`RegisterBody`/`SigninBody`, `routes/auth.ts`) run via `validate("json", …)` **before** the handler touches the `users` table or spends bcrypt time, so those caps are the only barrier stopping a malformed row (bad-shape email, blank/oversized display name) from reaching storage. Added 4 DFX Robustness cases over real HTTP + real file SQLite: register malformed `email` → 400 naming `email` **+ a well-formed register right after → 201** (recoverability); register over-length `name` (> 100) → 400 naming `name`; register empty `name` (`""`) → 400 naming `name` (the `min(1)` lower bound, distinct teeth from the max); signin malformed `email` → **400** naming `email` — deliberately asserted as 400 **not** the 401 credential path, pinning that validation fires before any credential/enumeration logic (orthogonal to the uniform-401 account-enumeration policy for *valid-but-unknown* emails, the open #46 register-enumeration Jalen decision — untouched). **Mutation-tested with perfect specificity:** dropping `RegisterBody.email`'s `.email()` / `name`'s `.max(100)` / `name`'s `.min(1)` and loosening `SigninBody.email`'s `.email()` each redden **exactly** their one case, no others. dfx 126 → 130 (+4 cases). Handler/schemas already correct → **gap in the tests, not the code**; test+docs only, no production change. One-round high-effort review clean; posted as a PR comment.

- 2026-07-08: **Test-gap audit merged — DFX Security for the `GET /v1/user` profile + stats aggregate tenant scoping (#390 → PR #391, merged).** Authored by an earlier loop run and left open with all 30 PR checks green but **no review round yet**; this run ran the mandated one-round high-effort adversarial review (two independent finder passes — **test-correctness/false-green** + **flakiness/cross-test-pollution** — both clean, posted as a PR comment), verified the new block passes in the daily suite locally (149/149) — which PR CI never runs — and **mutation-tested it live** (dropping `WHERE user_id` reddens **exactly** AC3, 148/149; restored → 149/149), then merged, closed #390, deleted the branch. `GET /v1/user` (`routes/user.ts:17-22`) returns the caller's profile **plus an aggregate stats block** — `stats.tasks` (`COUNT(CASE WHEN completed = 0 …)`, open tasks) + `stats.pomodoros` (`SUM(pomos_done)`) computed `WHERE user_id = :uid AND deleted_at IS NULL` — the app's **only user-facing cross-tenant _aggregate_**, with **zero** prior presence in the daily DFX suite (only the per-PR in-process api suite). A regression dropping the scope would silently fold another tenant's open-task + pomodoro counts into the caller's profile with **no status-code change** (still 200), invisible to every existing case. 3 cases over real HTTP + real file SQLite with fresh dedicated actors (owner: 3 open + 1 completed task, 2 pomodoros; other tenant: different non-zero 5/3): **AC1** authN (no token / garbage bearer → 401 never 500, valid → 200); **AC2** profile identity (response email/name/id are the token-owner's); **AC3** aggregate scope — owner's `stats.tasks` **exactly 3** (excludes the completed task *and* the other tenant's 5), `stats.pomodoros` **exactly 2** (never the other tenant's 3), with the other tenant's own 5/3 view as a positive control. Mutation-tested with perfect specificity (drop `WHERE user_id` → exactly AC3; `COUNT(*)` for `COUNT(CASE WHEN completed=0)` → exactly AC3). dfx 146 → 149. Handler already scoped → **gap in the tests, not the code**; test+docs only, no production change.

- 2026-07-08 (cont.): **DFX Reliability — wrong-kind template PATCH → 400 not 500 (#395 → PR #400, merged).** Authored by an earlier loop run and left open with all 30 checks green + a completed one-round high-effort adversarial code-review (clean, 0 findings, posted as a PR comment); this run sanity-checked the diff (small, backend-only), confirmed `CLEAN`/`MERGEABLE`, merged, closed #395, deleted the branch. `TemplateUpdateBodySchema.payload` is a bare `z.union([TemplatePayloadSchema, ProjectTemplatePayloadSchema])` — it accepts *either* kind's shape regardless of the template's actual kind. `PATCH /v1/templates/:id` then re-encodes the payload against the template's **effective kind** via `payloadSchemaFor(effectiveKind).parse(...)`; a payload matching the *other* kind passes the request validator, then throws a raw `ZodError` at the re-encode → since routes never throw `HTTPException`, `app.onError` treats it as a server fault → **500 INTERNAL_ERROR** (DFX Reliability violation: mismatched input must degrade to 4xx). (CREATE is safe — its union variants pin `kind`↔`payload`; only UPDATE decouples them.) Fix: `.parse()` → `.safeParse()` in the PATCH handler; on failure emit a clean **400 `VALIDATION_ERROR`** naming the offending `payload.*` field, mirroring the `validate()` middleware envelope. Backend-only; no contract/schema/migration change. Ships with 2 DFX Reliability integration cases (both directions + no-poison/row-unmutated) + api test + matrix update; adversarial review verified the two payload schemas' required fields are **disjoint** (`title` vs `name`) so a wrong-kind payload always fails the effective-kind re-parse (no accidental cross-kind validity).
- 2026-07-08 (cont.): **DFX Robustness — focus-session `started_at` bounded to ISO date-time, not any string (#402 → PR #403, merged).** Authored by an earlier loop run and left open with all 30 checks green + a completed one-round high-effort adversarial code-review (clean, 0 findings, posted as a PR comment); this run confirmed `CLEAN`/`MERGEABLE`, merged, closed #402, deleted the branch. `POST /v1/focus/sessions`'s route-local `FocusSessionBody` typed `started_at` as a bare `z.string().optional()` while the contract advertised an ISO date-time — any non-empty string (incl. `""` on a present field) was accepted and written straight into the `completed_entries` timestamp, a robustness gap on the one focus-log write path. Fix: bound `started_at` to the app-wide date-time **shape** regex (same precedent as `scheduled_start`/`remind_at`/`countdowns.date` — a superset that still accepts the `YYYY-MM-DDTHH:MM` app form), `.optional()` preserved so omission still 200s. Backend-only; no `@lumo/contracts` shape change (fix sits at the request boundary, matching the `scheduled_start` route-local precedent). Ships with 2 api cases + 4 DFX Robustness integration cases over real HTTP + real file SQLite (malformed→400 naming `started_at` / empty-string→400 / valid ISO round-trips / omitted→200) + matrix update; adversarial cross-file consumer trace verified no caller breaks (frontend completes via `api.completeTask`, never posting `started_at`; every backend/e2e caller sends `new Date().toISOString()`).
- 2026-07-11: **DFX Robustness — focus-session `duration` bounded to `[1,1440]` minutes, not unbounded (#405 → PR #406, merged).** Sibling to the #402 `started_at` bound on the same `POST /v1/focus/sessions` write path. The route validated `duration` (session minutes) as `z.number().int().min(1)` — **no upper bound** — so an oversized/overflow value was persisted verbatim into `completed_entries.duration`, silently poisoning the aggregate stats (`SUM(duration)` day/week totals). Fix: append `.max(1440)` (24h ceiling), matching the app-wide cap already on `tasks.duration` (`packages/contracts/src/task.ts:57`) and `template.payload.duration` (`.int().min(0).max(1440)`); the route keeps `.min(1)` (a recorded session is ≥ 1 min, vs the schemas' `.min(0)`). Backend-only; no `@lumo/contracts` shape change (fix at the request boundary, matching the `started_at` route-local precedent). Ships with api cases + DFX Robustness integration cases over real HTTP + real file SQLite (over-cap→400 naming `duration` + no-poison: completed-entry count unchanged **and** `pomos_done` stays 0 / the 1440 boundary → 200 / valid round-trip) + matrix update. **One-round high-effort review caught a real minor finding — fixed in-branch:** the hand-maintained OpenAPI spec (`routes/docs.ts:531`) still declared `duration` as a bare `integer` while the runtime schema now enforces `[1,1440]`; added `minimum: 1, maximum: 1440` + description so the published contract stays honest (posted as a PR comment). Adversarial consumer trace: no frontend/e2e caller posts `duration > 1440` (only real caller sends `25`). Handler otherwise correct → the missing bound was the sole defect; test+docs+one-line schema change, no behavioral change to valid inputs.
- 2026-07-11 (cont.): **Test-gap audit — DFX coverage for the `/v1/outlook` route family (#411 → PR #412, merged).** A live API-surface diff against the DFX matrix found the **entire `/v1/outlook` route family** (`routes/outlook.ts` — `GET /status`, `GET /calendar`) had **zero** daily-suite presence. It's an optional external integration (server-side Outlook/Graph calendar proxy), active only when configured, but three load-bearing production properties were unpinned. Added DFX cases over real HTTP + real file SQLite: **AC1 authN** — both routes reject a missing/blank `Authorization` with 401 before any Graph work; **AC2 fail-closed / graceful degradation** — with the integration unconfigured, `GET /calendar` short-circuits to a clean **503 `OUTLOOK_NOT_CONFIGURED` before any token/Graph fetch** (the config guard runs *ahead* of the missing-param 400, proving fail-closed not a 5xx availability footgun); **AC3 config confidentiality** — `GET /status` truthfully reports `configured:false` without leaking secret presence/shape. Handler verified already correct → gap in the tests, not the code; **test + docs only**, no production change. One-round high-effort adversarial review: clean, no findings (posted as PR comment). Matrix updated.

_Last restructured: 2026-06-24 (phase prioritization); status-synced 2026-06-28; issue-backlog reconciled 2026-07-05; #319 ticked 2026-07-06; #234 ticked 2026-07-06; #263 ticked 2026-07-06; #260 ticked 2026-07-07; #362 detail-surface polish merged 2026-07-07; #305 parse-bounds DFX merged 2026-07-07; #384 calendar-feed DFX merged 2026-07-07; #387 auth-input-bounds DFX merged 2026-07-07; #390 user-stats-aggregate DFX merged 2026-07-08; #395 template-PATCH-500→400 DFX merged 2026-07-08; #402 focus-started_at-bound DFX merged 2026-07-08; #405 focus-duration-bound DFX merged 2026-07-11; #411 outlook-route-family DFX merged 2026-07-11. Maintained by the autonomous loop each run._
